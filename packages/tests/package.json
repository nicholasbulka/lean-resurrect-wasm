{
  "name": "lean-wasm-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:node": "playwright test --project=node",
    "test:browser": "playwright test --project=browser",
    "test:headed": "playwright test --headed",
    "server": "node server.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
