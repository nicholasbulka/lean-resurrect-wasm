import type { Extension } from '@codemirror/state';

/**
 * Stub. Phase 3 replaces this with a StreamLanguage port of the Monarch
 * tokenizer in ../leanLanguage.ts.
 */
export function leanLanguage(): Extension {
  return [];
}
